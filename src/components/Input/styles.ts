import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#FFF",
        borderRadius: 8,
        borderColor: "#C3C5CB",
        borderWidth: 1,
        paddingHorizontal: 16,
        height: 48,
        width: "100%"
    },
});